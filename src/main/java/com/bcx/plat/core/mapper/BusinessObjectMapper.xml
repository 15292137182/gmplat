<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bcx.plat.core.mapper.BusinessObjectMapper">


    <select id="select" resultType="com.bcx.plat.core.entity.BusinessObject" parameterType="hashMap">
        SELECT
            row_id,object_code,object_name,relate_table_row_id,
            <include refid="com.bcx.plat.core.base.BaseMapper.common_columns"/>
        FROM t_business_object
        <where>
            <if test="strArr!=null and strArr.size()!=0">
                <foreach collection="strArr" item="str" index="index" open="(" close=")" separator="or">
                    object_code like '%${str}%' or object_name like '%${str}%' or relate_table_row_id like '%${str}%'
                </foreach>
            </if>
        </where>
    </select>

    <insert id="insert" parameterType="com.bcx.plat.core.entity.BusinessObject">
        INSERT INTO
                t_business_object
            (row_id,object_code,object_name,relate_table_row_id,
            <include refid="com.bcx.plat.core.base.BaseMapper.common_columns"/>)
        VALUES
            (#{rowId},#{objectCode},#{objectName},#{relateTableRowId},
        #{status},#{version},#{createUser},#{createUserName},#{createTime},#{modifyUser},#{modifyUserName},
        #{modifyTime},#{deleteUser},#{deleteUserName},#{deleteTime},#{deleteFlag}
        )
    </insert>

    <update id="ipdate" parameterType="com.bcx.plat.core.entity.BusinessObject">
        UPDATE
          t_business_object
            SET
        object_code=#{objectCode},
        object_name=#{objectName},
        relate_table_row_id=#{relateTableRowId},
        <include refid="com.bcx.plat.core.base.BaseMapper.common_columns_u"/>
        WHERE
            row_id = #{rowId}
    </update>

    <delete id="delete" parameterType="string">
        DELETE FROM t_business_object WHERE row_id=#{rowId}
    </delete>
</mapper>