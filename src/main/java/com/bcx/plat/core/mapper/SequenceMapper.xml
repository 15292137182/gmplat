<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bcx.plat.core.sqlmapper.SequenceMapper">

    <select id="currval" parameterType="string" resultType="int">
	SELECT sequence 
    FROM qrs_sequence  
    WHERE table_name =#{name}
	</select>


    <select id="getNextval" parameterType="string" resultType="int">
	SELECT sequence+1 
    FROM qrs_sequence  
    WHERE table_name =#{name}
	</select>

    <update id="nextval" parameterType="string">
	 UPDATE qrs_sequence  
     SET sequence = sequence+1  
     WHERE table_name = #{name}
	</update>


</mapper>